// The one place the version is written down. index.html loads this with a
// plain <script>, sw.js with importScripts, and the cache name is built from
// it — so bumping this line here is what ships an update.
self.APP_VERSION = '2.7.0';
