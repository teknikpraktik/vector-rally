// The one place the version is written down. index.html loads this with a
// plain <script>, shows it on the start page, and stamps it into every race
// state so a pasted state says which version made it.
self.APP_VERSION = '3.1.0';
